{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // For local development, you might override connection strings or other settings here
  // The Managed Identity connection string with DefaultAzureCredential fallback already handles local dev.
}